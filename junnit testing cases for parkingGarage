package SE-class-parking-garage

import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.junit.Assert;

public class parkingGarageTest { 

  public parkingGarageTest() {
    }
  
   @BeforeClass
   public static void setUpClass() {
   }
   
   @AfterClass
   public static void tearDownClass() {
   }
   
   @Test 
	public string testgetFloors() {
	int floors = 8;
	int expResult = 8;
	int result = SE-class-parking-garage.getFloors(floors);
	assertEquals(expResult, result);
	}
  
  @Test 
  public string testgetSpots() {
  int spots = 3,4;
  int expResult = 3,4;
  int result = SE-class-parking-garage.getSpots(Spots);
	assertEquals(expResult, result);
  }
  
  @Test 
  public string testgetArray() {
  return garage = true; 
  int expResult = true; 
  int result = SE-class-parking-garage.getArray(garage);
	assertEquals(expResult, result);
  
  @Test 
  public string testsetArray() {
  /* need to look more into how to script this part */
   
   @Test 
  public string testfillsSpot() {
  int result = 1;
  int expResult = 1;
  int result = SE-class-parking-garage.fillsSpot(garage[userFloor][userSpot]);
	assertEquals(expResult, result);
}
 @Test 
  public string testNumSpotsAvailble() {
  int i = 10;
  int expResult = 10;
  int result = SE-class-parking-garage.numSpotsAvailable(i);
	assertEquals(expResult, result);
}
 @Test 
  public string testisAvaible() {
  availability=true
  int expResult = true;
  int result = SE-class-parking-garage.isAvailable(int[][] garage, int userFloor, int userSpot);
	assertEquals(expResult, availability);
}
 @Test 
  public string testisAvaible2() {
  availability=false
  int expResult = false;
  int result = SE-class-parking-garage.isAvailable(int[][] garage, int userFloor, int userSpot);
	assertEquals(expResult, availability);
}
 
